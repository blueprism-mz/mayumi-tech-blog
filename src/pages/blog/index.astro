---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`${SITE_TITLE} | Blog`} description={SITE_DESCRIPTION} />
    <style>
      main {
        width: 960px;
      }
      .intro {
        width: 720px;
        max-width: calc(100% - 2em);
        margin: 2rem auto 1rem;
        padding: 0 1em;
        color: rgb(var(--gray-dark));
        line-height: 1.7;
      }
      .intro h1 {
        margin: 0 0 .5rem 0;
        line-height: 1.15;
      }
      .intro p {
        margin: .5rem 0;
      }

      ul {
        display: flex;
        flex-wrap: wrap;
        gap: 2rem;
        list-style-type: none;
        margin: 0;
        padding: 0;
      }
      ul li {
        width: calc(50% - 1rem);
      }
      ul li * {
        text-decoration: none;
        transition: 0.2s ease;
      }
      ul li a {
        display: block;
        padding: .25rem;
        border-radius: 12px;
      }
      ul li:first-child {
        width: 100%;
        margin-bottom: 1rem;
      }
      ul li:first-child img {
        width: 100%;
      }
      ul li:first-child .title {
        font-size: 2.369rem;
      }

      ul li img {
        margin-bottom: 0.5rem;
        border-radius: 12px;
      }

      .title {
        margin: 0;
        color: rgb(var(--black));
        line-height: 1.2;
      }
      .date {
        margin: .35rem 0 0 0;
        color: rgb(var(--gray));
      }
      .desc {
        margin: .5rem 0 0 0;
        color: rgb(var(--gray-dark));
        line-height: 1.6;
      }
      .tags {
        margin: .65rem 0 0 0;
        font-size: .9rem;
        color: rgb(var(--gray));
      }
      .tags span {
        margin-right: .6rem;
      }

      ul li a:hover .title,
      ul li a:hover .date {
        color: rgb(var(--accent));
      }
      ul li a:hover {
        box-shadow: var(--box-shadow);
      }

      @media (max-width: 720px) {
        ul {
          gap: 0.75em;
        }
        ul li {
          width: 100%;
        }
        ul li:first-child {
          margin-bottom: 0;
        }
        ul li:first-child .title {
          font-size: 1.563em;
        }
      }
    </style>
  </head>
  <body>
    <Header />
    <main>
      <section class="intro">
        <h1>Notes & experiments</h1>
        <p>
          新しい技術や未知の領域を試しながら、気づきと仮説を短く残しています。
          完成した結論より、思考の途中経過を大切にする公開ラボです。
        </p>
        <p style="color: rgb(var(--gray)); font-size: .95rem;">
          Tip: 記事が短くてもOK。あとで追記して育てる前提です。
        </p>
      </section>

      <section>
        <ul>
          {posts.map((post) => (
            <li>
              <a href={`/blog/${post.id}/`}>
                {post.data.heroImage ? (
                  <Image width={720} height={360} src={post.data.heroImage} alt="" />
                ) : null}

                <h4 class="title">{post.data.title}</h4>

                <p class="date">
                  <FormattedDate date={post.data.pubDate} />
                </p>

                {post.data.description ? (
                  <p class="desc">{post.data.description}</p>
                ) : null}

                {post.data.tags?.length ? (
                  <p class="tags">
                    {post.data.tags.map((t) => <span>#{t}</span>)}
                  </p>
                ) : null}
              </a>
            </li>
          ))}
        </ul>
      </section>
    </main>
    <Footer />
  </body>
</html>